# ─────────────────────────────────────────────
#  MCP Inbox — Environment Variables Example
#
#  Copy this file to .env and fill in your values.
#  Without a .env the app runs in demo mode automatically.
# ─────────────────────────────────────────────

# ── Telegram Bot (optional) ──────────────────
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# ── Gmail OAuth (optional) ───────────────────
# Place credentials.json and token.json in ./data/ (see docker-compose.yml)
GMAIL_CREDENTIALS_PATH=/app/data/credentials.json
GMAIL_TOKEN_PATH=/app/data/token.json
# Optional for serverless (Vercel): set JSON content directly (plain JSON or base64 JSON)
GMAIL_CREDENTIALS_JSON=
GMAIL_TOKEN_JSON=

# Gmail address to fetch unread messages for
GMAIL_USER=you@gmail.com

# ── Telegram Personal Account (optional) ─────
# Session file is generated automatically on first auth
TELEGRAM_SESSION_PATH=/app/data/telegram_personal.session

# Your Telegram API credentials from https://my.telegram.org
TELEGRAM_API_ID=your-api-id-here
TELEGRAM_API_HASH=your-api-hash-here
TELEGRAM_PHONE=+1234567890

# ── Database ─────────────────────────────────
DATABASE_PATH=/app/data/mcp_inbox.db

# ── Proxy (optional, e.g. Tor SOCKS5) ────────
# HTTPS_PROXY=socks5://127.0.0.1:9050
